subscription ClosedPullRequestWithChangelog {
  PullRequest(state: "closed") {
    number
    title
    labels @required {
      name @matches(re: "changelog:.*")
    }
    repo {
      owner
      name
      org {
        provider {
          url
        }
      }
    }
  }
}